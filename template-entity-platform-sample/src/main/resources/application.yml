server:
  port: 8080

spring:
  application:
    name: entity-platform-sample
  datasource:
    url: jdbc:h2:mem:entityplatform
    driver-class-name: org.h2.Driver
    username: sa
    password:
  h2:
    console:
      enabled: true
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true

app:
  template:
    enabled: true
    security:
      enabled: false
    audit:
      enabled: true
  entity-platform:
    api-prefix: /api
    scan-packages: com.lrenyi.template.entityplatform.sample
    enabled: true
